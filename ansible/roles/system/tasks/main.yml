---
- name: use English folder name
  shell: LANG=C xdg-user-dirs-update --force

- name: install system tools using apt
  become: yes
  apt:
    name:
      - htop
      - net-tools
      - ncdu
      - terminator
    update_cache: yes

- name: add fish ppa
  become: yes
  apt_repository:
    repo: ppa:fish-shell/release-3
    state: present

- name: install fish shell
  become: yes
  apt:
    name: fish
    update_cache: yes

- name: install fisher
  ansible.builtin.shell: curl -sL https://git.io/fisher | source && fisher install jorgebucaran/fisher
  args:
    executable: /bin/fish

- name: fisher update
  ansible.builtin.shell: fisher update
  args:
    executable: /bin/fish

- name: install mise
  shell: curl https://mise.jdx.dev/install.sh | sh

- name: mise tool install
  ansible.builtin.shell: source ~/.bashrc && mise install -y
  args:
    executable: /bin/bash
